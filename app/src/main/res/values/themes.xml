<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyBookingHotel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyBookingHotel" parent="Base.Theme.MyBookingHotel" />

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBlue</item>
        <item name="colorPrimaryDark">@color/colorBlue</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorWhite</item>
    </style>

    <style name="ToolbarTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="homeAsUpIndicator">@drawable/ic_back</item>
        <item name="colorAccent">@color/colorTextBlack</item>
        <item name="android:textSize">@dimen/size_title</item>
    </style>

    <style name="bottom_bottom_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/bottomBottomAnimation</item>
    </style>

    <style name="bottomBottomAnimation">
        <item name="android:windowEnterAnimation">@anim/bottom_up_300</item>
        <item name="android:windowExitAnimation">@anim/bottom_down_300</item>
    </style>

    <style name="DartNinjaProgressBarStyle" parent="@android:style/Widget.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/progress_indeterminate_dart_ninja
        </item>
        <item name="android:indeterminateDuration">2250</item>
    </style>
</resources>